 #include<stdio.h>
int main()
{
	int remainder,sum=0,i,a;
	printf("Enter the number:");
	scanf("%d",&a);
	for(i=1;i<a;i++)
	{
		remainder=a%i; 
		if(remainder==0)
		{
			sum=sum+i;
		}
	}
	if(a==sum)
	printf("%d is the perfect number",a);
	else
	printf("%d is not a perfect number",a);
return 0;
}
